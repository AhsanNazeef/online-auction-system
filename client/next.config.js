/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "img.freepik.com", "st.depositphotos.com", "wallpapers.com"],
  },
};

module.exports = nextConfig;
